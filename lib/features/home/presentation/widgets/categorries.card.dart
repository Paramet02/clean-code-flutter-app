import 'package:flutter/material.dart';

class CategorriesCard extends StatelessWidget {
  const CategorriesCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}